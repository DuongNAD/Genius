# Fully pinned: three machines (Windows dev, macOS hub, the grok worker) and
# CI all install from this file — loose `>=` pins let their environments
# drift apart silently. Bump versions deliberately, on one machine, and let
# the others pick the change up via git.
pydantic==2.12.5
tenacity==9.1.4
PyYAML==6.0.3
pytest==9.0.3
pathspec==1.1.1
tiktoken==0.12.0
httpx==0.28.1
python-dotenv==1.2.2
fastapi==0.135.1
uvicorn==0.41.0
flake8==7.3.0
websockets==16.0
requests==2.32.5
pytest-asyncio==1.4.0
# Per-test hang backstop (see pytest.ini `timeout`): a wedged WS/subprocess
# test must fail with stack dumps instead of hanging CI forever.
pytest-timeout==2.4.0
# Multi-language code-graph parsing (repo_graph edges, graph_index, the MCP
# code_graph tool): official tree-sitter core + per-grammar wheels. Optional
# at RUNTIME — ag_core.scanner.code_parse probes them with find_spec and
# falls back to Python-only ast parsing when absent — but pinned here so all
# three machines and CI exercise the same multi-language path. Deliberately
# NOT tree-sitter-language-pack: its 1.x line swapped in an undocumented
# PyO3 binding with a different node API.
tree-sitter==0.25.2
tree-sitter-javascript==0.25.0
tree-sitter-typescript==0.23.2
tree-sitter-go==0.25.0
# mac-branch divergence: uvloop event loop for the hub/agent servers and the
# CLI pipeline (installed via guarded uvloop.install() in serve.py /
# orchestrator.py; uvicorn.run entrypoints pick it up automatically).
# No Windows build exists — do NOT carry this line to the win branch.
uvloop==0.21.0
